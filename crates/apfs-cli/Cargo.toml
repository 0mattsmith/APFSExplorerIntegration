[package]
name = "apfs-cli"
description = "Inspect, list and extract files from APFS containers (images or block devices)"
version.workspace = true
edition.workspace = true
license = "MIT OR Apache-2.0"


[dependencies]
apfs-core = { path = "../apfs-core" }

[[bin]]
name = "apfs"
path = "src/main.rs"
